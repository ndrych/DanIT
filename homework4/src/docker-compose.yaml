version: "3.9"
services:
  jenkins:
    image: jenkins/jenkins

    ports:
      - 8082:8080
      - 50000:50000
    volumes:
      - ./jenkins_home:/var/jenkins_home
      #- /var/run/docker.sock:/var/run/docker.sock
      #user: "${UID}:${GID}"
                                                      #
